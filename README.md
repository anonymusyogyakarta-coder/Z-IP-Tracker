# Z-IP-Tracker
### 🚀 CARA INSTALL (TERMUX)
```bash
pkg update && pkg upgrade -y && pkg install git -y && git clone [https://github.com/anonymusyogyakarta-coder/Z-IP-Tracker](https://github.com/anonymusyogyakarta-coder/Z-IP-Tracker) && cd Z-IP-Tracker && chmod +x setup.sh && ./setup.sh && python Z-IP.py
